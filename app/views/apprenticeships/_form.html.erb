<%= form_for(@apprenticeship) do |f| %>
  <% if @apprenticeship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apprenticeship.errors.count, "error") %> prohibited this apprenticeship from being saved:</h2>

      <ul>
      <% @apprenticeship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
 <!--    <%#= f.label :teacher_id %><br>
    <%#= f.number_field :teacher_id %> -->
    <%= f.select :teacher_id,Teacher.all.collect {|p| [p.name,p.id]}%>
  </div>
  <div class="field">
  <!--   <%#= f.label :student_id %><br>
    <%#= f.number_field :student_id %> -->
    <%= f.select :student_id,Student.all.collect{|p| [p.name,p.id]}%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
